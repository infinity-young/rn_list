export function getID() {
    let id;
    // listContainers().then(pages => {
    //     let url = pages.pop().url;
    //     let urlArray = url.split('&');
    //     for (let index = 0; index < urlArray.length; index++) {
    //         if (urlArray[index].indexOf('topListId')) {
    //             id = urlArray[index].split('=').pop();
    //         }
    //     }
    // });
    //to do
    console.log("========获取城市id==")
    return id;
}
